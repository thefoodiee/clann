import 'package:flutter/material.dart';

class ScanTimetableScreen extends StatelessWidget {
  const ScanTimetableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
